📄 Level 1/01. Deploy Pods in Kubernetes Cluster.md
markdown
# Task 01: Deploy Pods in Kubernetes Cluster

## Scenario
The Nautilus DevOps team is diving into Kubernetes for application management. One team member has a task to create a pod according to the details below:

- Pod name: **pod-nginx**
- Image: **nginx:latest**
- Label: **app=nginx_app**
- Container name: **nginx-container**

Note: The `kubectl` utility on `jump_host` is configured to operate with the Kubernetes cluster.

---

## pod-nginx.yaml

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: pod-nginx
  labels:
    app: nginx_app
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
Apply the Pod
bash
kubectl apply -f pod-nginx.yaml
Verify
bash
kubectl get pods -o wide
kubectl describe pod pod-nginx
User's Edge Browser Tabs Metadata
The tab with IsCurrent=true is the user's currently active/viewing tab, while tabs with IsCurrent=false are other open tabs in the background.

json
edge_all_open_tabs = [
  {
    "pageTitle": "kodekloud-kubernetes-challenge/Level 1/01. Deploy Pods in Kubernetes Cluster.md at main · mirakib/kodekloud-kubernetes-challenge",
    "pageUrl": "https://github.com/mirakib/kodekloud-kubernetes-challenge/blob/main/Level%201/01.%20Deploy%20Pods%20in%20Kubernetes%20Cluster.md",
    "tabId": 2055812882,
    "isCurrent": true
  }
]
